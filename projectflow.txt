1. Create a git repo and clone it in local
2. Create my_code.py and add code to it. (it will save new csv file to a new "data" folder)
3. do a git commit push before inititating a DVC.
4.pip install DVC
5. Now we do "dvc init" (creates .dvcignore, .dvc)